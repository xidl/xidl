export const {{ schema_name }} = z.object({
    {% for param in params %}    {{ param.prop }}: {% if param.optional %}{{ param.schema }}.optional(){% else %}{{ param.schema }}{% endif %},
    {% endfor %}});
export type {{ name }} = z.infer<typeof {{ schema_name }}>;
