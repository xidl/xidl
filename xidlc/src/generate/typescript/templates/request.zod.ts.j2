{% from "macros.j2" import render_zod %}
export const {{ schema_name }}Schema = z.object({
    {% for param in params %}    {{ param.prop }}: {{ render_zod(param.schema) }}{% if param.optional %}.optional(){% endif %},
    {% endfor %}});
export type {{ name }} = z.infer<typeof {{ schema_name }}Schema>;
