{% from "macros.j2" import render_type %}
{% if doc and doc | length > 0 %}
    /**
    {% for line in doc %} * {{ line }}
    {% endfor %} */
{% endif %}
export type {{ name }} = {{ render_type(type_expr) }};
