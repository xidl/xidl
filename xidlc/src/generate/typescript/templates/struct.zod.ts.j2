export const {{ schema_name }} = z.object({
    {% for field in fields %}    {{ field.prop }}: {% if field.xjson_meta %}xjson({{ field.schema }}{% if field.optional %}.optional(){% endif %}, {{ field.xjson_meta }}){% else %}{{ field.schema }}{% if field.optional %}.optional(){% endif %}{% endif %},
    {% endfor %}});
export type {{ ident }} = z.infer<typeof {{ schema_name }}>;
