export const {{ schema_name }} = z.object({
    {% for field in fields %}    {{ field.prop }}: {{ field.schema }}{% if field.optional %}.optional(){% endif %},
    {% endfor %}});
export type {{ ident }} = z.infer<typeof {{ schema_name }}>;
