{%- for line in doc %}
    /// {{ line }}
{%- endfor %}
{% for attr in rust_attrs %}
    #[{{ attr }}]
{% endfor %}
#[derive(Debug, Clone)]
pub struct {{ ident }} {
    {% for member in members %}
        {% if member.rust_attrs and member.rust_attrs | length > 0 %}
            {% for attr in member.rust_attrs %}
                #[{{ attr }}]
            {% endfor %}
        {% endif %}
        pub {{ member.name }}: {{ member.ty }},
    {% endfor %}
}
