---
title: "XIDL"
description: "一个面向现代分布式系统的 IDL 工具链：一次定义，生成多种协议、代码和规范。"
template: splash
hero:
  tagline: "用一份 IDL 驱动 HTTP、JSON-RPC、Rust 代码、OpenAPI、OpenRPC 和更多目标。"
  actions:
    - text: "从指南开始"
      link: ./guide/
      icon: right-arrow
      variant: primary
    - text: "查看详细文档"
      link: ./docs/
      icon: document
    - text: "阅读 RFC"
      link: ./rfc/
      icon: external
---

import { Card, CardGrid } from '@astrojs/starlight/components';

## 为什么是 XIDL

XIDL 不是单纯的“代码生成器”，而是一套围绕契约定义构建的工作流：
你先写 IDL，再由同一份契约生成服务端、客户端、Schema、辅助运行时和
工具链产物。

<CardGrid stagger>
  <Card title="一份契约，多种输出" icon="seti:document">
    同一份 IDL 可以生成 Rust 类型、Axum HTTP 接口、JSON-RPC 绑定、
    OpenAPI、OpenRPC，以及面向调试和工具链的中间产物。
  </Card>
  <Card title="协议规则显式化" icon="seti:gear">
    HTTP 路由、参数来源、流式交互和安全声明直接写在契约里，而不是散落在
    各个运行时框架和注释约定中。
  </Card>
  <Card title="对人和工具都友好" icon="seti:search">
    结构化 IDL 既方便团队评审，也方便生成器、编辑器、语言服务器和 AI 工具
    理解与处理。
  </Card>
  <Card title="先学主线，再扩展" icon="seti:rocket">
    文档主线以 `xidlc` 为入口，用最短路径带你跑通第一个 HTTP + Rust 例子；
    其他目标和协议再按需深入。
  </Card>
</CardGrid>

## 你可以用它做什么

- 定义 HTTP API，并生成 `rust-axum` 服务端/客户端代码。
- 定义 JSON-RPC 接口，并生成 `rust-jsonrpc` 绑定与 `openrpc.json`。
- 从同一份契约生成 OpenAPI，避免代码实现和文档规范漂移。
- 让生成流程进入 Rust `build.rs`，或者直接通过 `xidlc` 手动生成产物。
- 为流式 API 和安全策略建立可审查、可追踪的显式声明。

## 从哪里开始

- 第一次接触 XIDL：看[指南总览](./guide/)和[快速开始](./guide/quickstart/)。
- 想先跑通 HTTP + Rust：看[第一个 HTTP API](./guide/first-http-api/)。
- 想理解 CLI、语言和目标生成器：看[详细文档](./docs/)。
- 想确认精确语义和映射规则：看[RFC](./rfc/)。

## 推荐路径

1. 安装 `xidlc`。
2. 写一个最小 `.idl` 文件。
3. 用 `xidlc` 生成 `rust-axum` 或 `openapi`。
4. 再回到详细文档查询语法、注解和目标差异。

## 典型使用场景

- 团队想把 HTTP 接口、客户端、Schema 文档收敛到同一份契约。
- Rust 服务需要稳定地生成类型和 Axum 接口层，但又不想把契约绑定死在框架代码里。
- 需要同时维护 HTTP 和 JSON-RPC 风格的 API，但不希望使用两套完全割裂的建模方式。
- 想让编辑器工具、生成器和自动化脚本都围绕同一份结构化定义工作。

## 文档分区

- [Guide](./guide/)：新手入口，强调最低学习成本。
- [Docs](./docs/)：详细说明和参考资料。
- [RFC](./rfc/)：规范性说明，定义精确映射和约束。
