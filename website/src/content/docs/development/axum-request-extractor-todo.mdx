---
title: "Axum Unary Parameter Expansion TODO"
description: "Checklist for removing unary Request<T> wrappers from rust-axum server traits."
---

## Phase 0

- confirm phase 1 only targets unary HTTP handlers
- confirm streaming handlers keep the current shape
- confirm auth is exposed as an explicit generated parameter in phase 1

## Phase 1

- audit `xidlc/src/generate/rust_axum/interface.rs`
- separate trait-signature needs from adapter-only transport helpers
- verify `MethodContext.params` is enough for unary rendering

## Phase 2

- update unary trait generation in `interface.rs.j2`
- keep streaming methods unchanged
- remove public unary `FooRequest` exposure where no longer needed

## Phase 3

- rewrite unary server adapters to call `svc.method(arg1, arg2, ...)`
- preserve media-type, path, query, header, cookie, body, and auth behavior

## Phase 4

- remove dead template branches and now-unused unary request context fields
- decide whether `Request<T>` becomes stream-only documentation-wise

## Phase 5+

- regenerate snapshots
- add focused regression coverage
- update user docs after generator behavior changes
