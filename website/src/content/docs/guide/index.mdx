---
title: "Install & Quickstart"
description: "Install xidlc, verify the CLI, and generate your first output."
---

import { TabItem, Tabs } from '@astrojs/starlight/components';

The XIDL compiler, `xidlc`, compiles `.idl` files containing interface
definitions into server code, client code, and protocol schemas.

## 1. Install `xidlc`

<Tabs>
  <TabItem label="macOS / Linux">

```bash
curl --proto '=https' --tlsv1.2 -sSf https://xidl.netlify.app/install.sh | sh
```

  </TabItem>
  <TabItem label="Windows PowerShell">

```bash
iwr -useb https://xidl.netlify.app/install.ps1 | iex
```

  </TabItem>
  <TabItem label="Cargo">

```bash
cargo install xidlc
```

  </TabItem>
  <TabItem label="Cargo Binstall">

```bash
cargo binstall xidlc
```

  </TabItem>
</Tabs>

## 2. Verify the install

```bash
xidlc --help
```

## 3. Generate a first artifact

```bash
xidlc gen -o out rust your.idl
```

## 4. Built-in targets

- `rust`
- `rust-axum`
- `rust-jsonrpc`
- `openapi`
- `openrpc`
- `typescript`
- `typescript-rest`
- `go`
- `go-rest`

See [Target Generators](../docs/targets/) for the full list with aliases and
typical combinations.

## Editor Support

A Visual Studio Code extension provides syntax highlighting, code formatting,
and HTTP interface preview. See [Editor](./editor/) for installation and
details.

## Next Steps

- Continue with [Your First HTTP API](./first-http-api/).
- Read [Using xidlc](../docs/xidlc/).
- Browse [Supported Targets](../docs/targets/).
